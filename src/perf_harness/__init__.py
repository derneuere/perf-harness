"""perf-harness: scaffolding + tool checks for the perf-harness skill.

The actual optimization loop lives in skill/SKILL.md. This package only
sets up directories, copies template files, and verifies tool availability.
"""

__version__ = "0.1.0"
